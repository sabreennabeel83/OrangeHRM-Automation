package org.example;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.time.Duration;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
//    protected WebDriver driver;
//
//     public Main(){
//         driver= new ChromeDriver();
//         driver.get("https://opensource-demo.orangehrmlive.com/web/index.php/auth/login");
//         driver.manage().window().maximize();
//
//     }
//
//     private void Login(String username , String password){
//
//         WebDriverWait waitDriverI = new WebDriverWait(driver, Duration.ofSeconds(5));
//         waitDriverI.until(ExpectedConditions.visibilityOfElementLocated(By.xpath("//input[@name='username']")));
//
//         driver.findElement(By.xpath("//input[@name='username']")).sendKeys(username);
//
//         WebDriverWait waitDriver = new WebDriverWait(driver, Duration.ofSeconds(5));
//         waitDriver.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector("input[placeholder='Password']")));
//
//         driver.findElement(By.cssSelector("input[placeholder='Password']")).sendKeys(password);
//         driver.findElement(By.xpath("//button[@type='submit']")).click();
//
//     }
//


    public static void main(String[] args) {

//         Main m = new Main();
//         m.Login("Admin" ,"admin123");





    }
}